import React from "react";
import { View } from "react-native";
import { Header } from "~/components";

const Home: React.FC = () => {
  return (
    <View style={{ flex: 1 }}>
      <Header />
    </View>
  );
};

export default Home;
